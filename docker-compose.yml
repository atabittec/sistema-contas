services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: "file:/app/data/prod.db"
      SESSION_SECRET: "${SESSION_SECRET}"
    volumes:
      - ./data:/app/data
